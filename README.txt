to run:

- (bash)npm install
- (bash) mongod --dbpath=./data/ --port 27018
- (terminal) mongo --port 27018 (for checking the database)
- (new bash) npm start
- (browser) localhost:3000

<can import hardcoded database with code in test-script for users and posts>